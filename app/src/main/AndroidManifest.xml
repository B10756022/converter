<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmi">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity"><!--打開出現歡迎畫面-->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:screenOrientation="portrait"
            >
        </activity><!--跳轉要加上-->
        <activity android:name=".HomeActivity"/><!--跳轉要加上-->
        <activity android:name=".RecordActivity"/><!--跳轉要加上-->
        <activity android:name=".ResultActivity"/><!--跳轉要加上-->
        <activity android:name=".BmiActivity"/><!--跳轉要加上-->
        <activity android:name=".ShoeManActivity"/><!--跳轉要加上-->
        <activity android:name=".ShoeWomanActivity"/><!--跳轉要加上-->
        <activity android:name=".TimeActivity"/><!--跳轉要加上-->
        <activity android:name=".TemperatureActivity"/><!--跳轉要加上-->
        <activity android:name=".MoneyActivity"/><!--跳轉要加上-->
    </application>

</manifest>